'function plotData() { &tab;&tab;var canvas; &tab;&tab;var ctx; &tab;&tab;&tab;var maxPass=Math.max.apply(Math,dataPass);&tab;var maxFail=Math.max.apply(Math,dataFail);&tab;var maxArray=[maxPass, maxFail];&tab;var max=Math.max.apply(Math,maxArray);&tab;&tab;max=Math.round(max / 10) * 100;&tab;&tab;canvas = document.getElementById("canvas"); &tab;&tab;ctx = canvas.getContext("2d"); &tab;&tab;ctx.clearRect(0, 0, canvas.width, canvas.height); &tab;&tab;&tab;&tab;var xpos=200;&tab;var ypos=50;&tab;var height=10;&tab;var gapMajor=20;&tab;&tab;ctx.fillStyle = "blue";&tab;&tab;ctx.lineWidth = "2.0";&tab;&tab;ctx.beginPath();&tab;ctx.moveTo(xpos-2, ypos);&tab;ctx.lineTo(xpos-2, canvas.height);&tab;ctx.closePath();&tab;ctx.stroke();&tab;&tab;ctx.beginPath();&tab;ctx.moveTo(xpos, ypos);&tab;ctx.lineTo(canvas.width, ypos);&tab;ctx.closePath();&tab;ctx.stroke();&tab;&tab;var tickerx=xpos;&tab;var tickery=ypos;&tab;for(var i=0 ; i!=max ; i++){&tab;&tab;ctx.beginPath();&tab;ctx.moveTo(tickerx, tickery-5);&tab;ctx.lineTo(tickerx, tickery);&tab;ctx.closePath();&tab;ctx.stroke();&tab;&tab;ctx.textAlign = "left";&tab;ctx.fillStyle = "blue";&tab;ctx.fillText( (i*max/50), tickerx, tickery-10);&tab;&tab;tickerx+=max/5;&tab;}&tab;&tab;ctx.fillStyle = "red";&tab;&tab;ctx.fillRect(canvas.width - 40 , 0+100, 10, 10);&tab;ctx.textAlign = "start";&tab;ctx.fillStyle = "red";&tab;ctx.fillText("Fail", canvas.width - 25 , 0+110);&tab;&tab;ctx.fillStyle = "green";&tab;&tab;ctx.fillRect(canvas.width - 40 , 0+120, 10, 10);&tab;ctx.textAlign = "start";&tab;ctx.fillStyle = "green";&tab;ctx.fillText("Pass", canvas.width - 25 , 0+130);&tab;&tab;ypos+=5;&tab;for (var i = 0; i != series.length; i++) { &tab;&tab;&tab;&tab;ctx.textAlign = "end";&tab;ctx.fillStyle = "blue";&tab;ctx.fillText(series[i], xpos-10, ypos+10); &tab;&tab;ctx.fillStyle = "red";&tab;&tab;ctx.fillRect(xpos , ypos , dataFail[i] * 10, height);&tab;&tab;ypos +=height;&tab;ctx.fillStyle = "green";&tab;&tab;ctx.fillRect(xpos, ypos , dataPass[i] * 10, height);&tab;&tab;ypos+=gapMajor;&tab;&tab;&tab;} &tab;&tab;} &tab;&tab;plotData();'